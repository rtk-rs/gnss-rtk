//! Position solving candidate
use itertools::Itertools;
use std::cmp::Ordering;

use crate::prelude::{Candidate, Carrier};

impl Candidate {
    /// Runs the SD algorithm between [Self] and rhs
    pub(crate) fn sd_mut(&mut self, rhs: &Self) {

    }
}
